const campaignKeys=['utm_source','utm_medium','utm_campaign','utm_content','utm_term','gclid','fbclid'];const campaign=Object.fromEntries(campaignKeys.map(k=>[k,new URLSearchParams(location.search).get(k)]).filter(([,v])=>v));if(Object.keys(campaign).length)sessionStorage.setItem('commeo_campaign',JSON.stringify(campaign));const menu=document.querySelector('.menu');const nav=document.querySelector('nav');menu?.addEventListener('click',()=>{nav.classList.toggle('open');menu.textContent=nav.classList.contains('open')?'×':'☰'});document.querySelectorAll('nav a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));
